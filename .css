.particle-container {
    position: absolute;
    width: 100vw;
    height: 100vh;
    top: 0;
    left: 0;
    overflow: hidden; /* Zamezit přetečení částic mimo viewport */
}

.particle {
    width: 5px;
    height: 5px;
    background-color: white;
    border-radius: 100%; /* Oprava, aby částice byly kruhové */
    opacity: 0.1;
    outline: 1px solid rgba(0, 0, 0, 0.3);
    position: absolute; /* Ujisti se, že částice mají absolutní pozici */
}

@keyframes move {
    from {
        transform: translate(0, 0);
    }
    to {
        transform: translate(100vw, 100vh);
    }
}